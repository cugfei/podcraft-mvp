# PodCraft MVP 开发任务清单（V2.0）

> 版本：V2.0  
> 日期：2026-05-25  
> 依据：`PodCraft_AI播客产品设计方案_优化版_V3.0.md` + `prototype/` 原型  
> 原则：**先跑通 MVP 核心闭环，每个任务 ≤ 1 个开发 session**

---

## 📋 总览

| 阶段 | 任务数 | 预估 Sessions |
|------|--------|--------------|
| Phase 1：基础架构 | 8 | 8 |
| Phase 2：静态页面 | 8 | 8 |
| Phase 3：交互逻辑 | 10 | 10 |
| Phase 4：数据存储与 API | 10 | 10 |
| Phase 5：用户系统与计费 | 6 | 6 |
| Phase 6：集成联调 | 4 | 4 |
| **合计** | **46** | **46** |

---

## Phase 1：基础架构

> 目标：搭建项目骨架，定义数据模型，建立开发规范

### T-1.1 初始化前后端项目

| 字段 | 内容 |
|------|------|
| **任务名称** | 初始化前后端项目 |
| **目标** | 建立前后端基础工程结构，确保后续任务有统一落点 |
| **输入** | PRD §10 技术架构：Next.js 14 + TypeScript + Tailwind CSS（前端），FastAPI + SQLAlchemy + PostgreSQL/SQLite（后端） |
| **输出** | 可启动的前端项目、后端项目、基础 README |
| **依赖项** | 无 |
| **完成标准** | ① 前端 `pnpm dev` 可启动并显示默认页面 ② 后端 `uvicorn` 可启动并提供 `GET /api/health` ③ README 写明启动命令 ④ `.env.example` 存在且不含真实密钥 ⑤ 前端已配置 Tailwind CSS + MUI ⑥ 后端已配置 CORS + SQLite 开发模式 |
| **模块** | 全局 |
| **角色** | 前端 + 后端 |

### T-1.2 定义核心数据模型与迁移

| 字段 | 内容 |
|------|------|
| **任务名称** | 定义核心数据模型与迁移 |
| **目标** | 建立所有核心 ORM 模型，确保 PRD §8.2 表设计完整映射 |
| **输入** | PRD §8.2 表设计草案：users, credit_accounts, credit_transactions, orders, voice_presets, podcast_projects, podcast_roles, podcast_segments, synthesis_tasks, audio_assets |
| **输出** | SQLAlchemy models + Alembic migration |
| **依赖项** | T-1.1 |
| **完成标准** | ① 包含全部 10 张核心表的 ORM 模型 ② `voice_presets` 包含 V3.0 新增字段（language, voice_params, is_cloned, clone_source, usage_count） ③ `podcast_segments.emotion` 支持 V3.0 情感标记 ④ `alembic upgrade head` 可执行 ⑤ 本地 SQLite 可创建全部表 |
| **模块** | 数据库设计 |
| **角色** | 后端 |

### T-1.3 实现统一 API 响应格式与错误处理

| 字段 | 内容 |
|------|------|
| **任务名称** | 实现统一 API 响应格式与错误处理 |
| **目标** | 为前后端联调建立稳定 API 基础 |
| **输入** | PRD §11.3 失败处理表 |
| **输出** | 统一响应模型、异常类、中间件 |
| **依赖项** | T-1.1 |
| **完成标准** | ① 成功响应格式：`{"code": 0, "data": {...}, "message": "ok"}` ② 错误响应格式：`{"code": 4xx/5xx, "data": null, "message": "错误描述"}` ③ 包含全局异常处理器 ④ 包含请求日志中间件 ⑤ Pydantic schema 验证错误自动转换为统一格式 |
| **模块** | API 接口 |
| **角色** | 后端 |

### T-1.4 前端路由与布局骨架

| 字段 | 内容 |
|------|------|
| **任务名称** | 前端路由与布局骨架 |
| **目标** | 建立前端页面路由和公共布局组件 |
| **输入** | PRD §4 信息架构，原型 topbar 导航设计 |
| **输出** | Next.js App Router 页面结构 + 公共布局组件 |
| **依赖项** | T-1.1 |
| **完成标准** | ① 路由结构：`/`（首页）、`/create`（创建播客）、`/editor/:id`（编辑器）、`/podcasts`（我的播客）、`/voices`（声音工坊）、`/login`、`/register`、`/admin` ② TopBar 组件含 Logo、导航、余额、充值按钮 ③ 侧边栏（可选）④ 所有页面可访问且显示占位内容 |
| **模块** | 首页 |
| **角色** | 前端 |

### T-1.5 TTS Provider 适配层设计

| 字段 | 内容 |
|------|------|
| **任务名称** | TTS Provider 适配层设计 |
| **目标** | 实现 TTS Provider 抽象接口，支持多 Provider 切换 |
| **输入** | PRD §7.4 TTS Provider 抽象：`synthesize(text, voice_id, speed, pitch, volume, emotion, language, voiceEffects, soundEffect)` |
| **输出** | TTS Provider 基类 + MiniMax 实现 + Edge-TTS 实现 |
| **依赖项** | T-1.2 |
| **完成标准** | ① `BaseTTSProvider` 抽象类定义 `synthesize()` 方法 ② `MiniMaxProvider` 实现可调用 MiniMax Speech-2.8-turbo API ③ `EdgeTTSProvider` 实现可调用 Edge-TTS ④ 支持语音参数（speed/pitch/volume/emotion/language）透传 ⑤ 支持 Provider 配置管理（API Key、QPS 限制） ⑥ 包含 Provider 降级逻辑框架 |
| **模块** | API 接口 |
| **角色** | 后端 |

### T-1.6 Celery 任务队列配置

| 字段 | 内容 |
|------|------|
| **任务名称** | Celery 任务队列配置 |
| **目标** | 实现异步任务队列，支持长耗时合成任务 |
| **输入** | PRD §7.3 音频合成流程：异步生成 + 状态追踪 |
| **输出** | Celery + Redis 配置 + 基础 task 定义 |
| **依赖项** | T-1.1 |
| **完成标准** | ① Celery worker 可启动 ② Redis 连接正常 ③ 包含 `synthesize_segment_task` 和 `synthesize_full_task` 两个任务骨架 ④ 任务状态可通过 `GET /api/synthesis-tasks/:id` 查询 ⑤ 包含重试策略（指数退避 10s/30s/90s，最多 3 次） |
| **模块** | API 接口 |
| **角色** | 后端 |

### T-1.7 对象存储与 FFmpeg 集成

| 字段 | 内容 |
|------|------|
| **任务名称** | 对象存储与 FFmpeg 集成 |
| **目标** | 实现音频文件存储和拼接能力 |
| **输入** | PRD §7.3 音频拼接：50ms crossfade + 响度归一化 |
| **输出** | 存储服务类 + FFmpeg 工具类 |
| **依赖项** | T-1.1 |
| **完成标准** | ① MinIO/本地文件存储服务可上传/下载/删除 ② FFmpeg 可执行音频拼接（多段 MP3 → 完整 MP3） ③ 包含 50ms crossfade 拼接 ④ 包含响度归一化（loudness normalization） ⑤ 包含停顿插入（基于 `<#N#>` 标签解析） |
| **模块** | API 接口 |
| **角色** | 后端 |

### T-1.8 前端设计系统与组件库

| 字段 | 内容 |
|------|------|
| **任务名称** | 前端设计系统与组件库 |
| **目标** | 基于原型建立统一设计令牌和基础组件 |
| **输入** | 原型 CSS 变量（design tokens）：`--brand: #0b0b0d`, `--success: #10b981`, `--radius: 12px` 等 |
| **输出** | Tailwind 配置 + 基础组件库 |
| **依赖项** | T-1.1 |
| **完成标准** | ① Tailwind 配置含原型全部 design tokens（颜色/圆角/阴影/字体） ② 基础组件：Button（primary/outline/danger）、Card、Input、Select、Slider、Toggle、Badge、Modal、Toast ③ 响应式断点配置（mobile/tablet/desktop） ④ Storybook 或组件预览页可查看所有组件 |
| **模块** | 首页 |
| **角色** | 前端 + 设计 |

---

## Phase 2：静态页面

> 目标：完成所有页面的静态 UI 实现，不接真实数据

### T-2.1 首页（Landing Page）

| 字段 | 内容 |
|------|------|
| **任务名称** | 首页（Landing Page） |
| **目标** | 基于 prototype/index.html 实现首页 UI |
| **输入** | prototype/index.html：Hero 区域 + 特性卡片 + 价格区域 + Footer |
| **输出** | `/` 路由首页组件 |
| **依赖项** | T-1.4, T-1.8 |
| **完成标准** | ① Hero 区域含标题、副标题、CTA 按钮 ② 4 个特性卡片（价格优惠/最新模型/多语言支持/简单易用） ③ 价格区域（免费试用/按量计费） ④ Footer 含版权信息 ⑤ 移动端响应式适配 ⑥ 品牌名从 Aura Studio 替换为 PodCraft |
| **模块** | 首页 |
| **角色** | 前端 |

### T-2.2 创建播客页

| 字段 | 内容 |
|------|------|
| **任务名称** | 创建播客页 |
| **目标** | 实现播客创建表单 UI |
| **输入** | PRD §6.1 创建播客页：输入方式 + 生成引导 + 播客模式 + 角色模板 + 音色预选 |
| **输出** | `/create` 路由创建页组件 |
| **依赖项** | T-1.4, T-1.8 |
| **完成标准** | ① 输入方式：主题输入（text）、文档粘贴（textarea ≤ 5000 字）、URL 输入 ② 播客模式：单人/双人/长文解读 三选一 ③ 生成引导：重点/角度标签、风格标签（专业/轻松/故事/新闻）、目标时长 ④ 双人模式含角色模板选择（主持人+专家/辩论双方/好友闲聊） ⑤ 每个角色含音色选择下拉 ⑥ 预计消耗积分展示区 ⑦ "生成播客"按钮 |
| **模块** | 详情页 |
| **角色** | 前端 |

### T-2.3 播客编辑器页（左侧文稿区）

| 字段 | 内容 |
|------|------|
| **任务名称** | 播客编辑器页（左侧文稿区） |
| **目标** | 实现文稿编辑器左侧面板 UI |
| **输入** | PRD §6.2 编辑器页 + prototype/synthesis.html 文本输入区 + 工具栏 |
| **输出** | `/editor/:id` 路由编辑器页左侧组件 |
| **依赖项** | T-1.4, T-1.8 |
| **完成标准** | ① 片段列表：每段显示角色标识 + 文本内容 + 状态标签 ② 角色颜色区分（A 绿色/B 蓝色） ③ 每段右侧含操作按钮：🪄 AI优化、🗑️ 删除、🔄 重试（失败时） ④ 底部字数统计栏：字数 + 预计时长 + 预计消耗积分 + 余额不足提示 ⑤ 快捷工具栏：短停顿/中停顿/长停顿/多音字/语气词/词典开关 ⑥ textarea 分段编辑模式 |
| **模块** | 详情页 |
| **角色** | 前端 |

### T-2.4 播客编辑器页（右侧设置面板）

| 字段 | 内容 |
|------|------|
| **任务名称** | 播客编辑器页（右侧设置面板） |
| **目标** | 实现编辑器右侧音色与参数设置面板 UI |
| **输入** | prototype/synthesis.html 设置面板：音色/语言增强/音色调节/声音效果器/音效/保存配置 |
| **输出** | `/editor/:id` 路由编辑器页右侧组件 |
| **依赖项** | T-1.4, T-1.8 |
| **完成标准** | ① 设置/历史 Tab 切换 ② 音色选择区：官方音色/克隆音色 Tab + 音色下拉 ③ 语言/方言增强下拉（自动检测/普通话/粤语/四川话/东北话/English 等） ④ 音色调节：语速 Slider（0.5-2.0）、音调 Slider（-12~+12）、音量 Slider（0.0-2.0） ⑤ 情绪风格下拉（自动/开心/悲伤/愤怒/惊讶/平静） ⑥ 保存当前参数功能 |
| **模块** | 详情页 |
| **角色** | 前端 |

### T-2.5 播客对话合成页（interview 模式）

| 字段 | 内容 |
|------|------|
| **任务名称** | 播客对话合成页（interview 模式） |
| **目标** | 基于 prototype/interview.html 实现双人对话合成 UI |
| **输入** | prototype/interview.html：角色 A/B 音色选择 + 对话文案批量输入 + 对话列表 + 历史任务 |
| **输出** | `/interview` 路由对话合成页组件（或集成到创建页） |
| **依赖项** | T-1.4, T-1.8 |
| **完成标准** | ① 角色 A/B 音色选择（含中英文音色） ② 批量文案输入区（自动解析 A:/B: 格式） ③ 对话条目列表（角色选择 + 文本编辑 + 删除） ④ 字符计数（含上限 10000 提示） ⑤ 提交合成任务按钮 ⑥ 历史任务列表（含状态标签） |
| **模块** | 详情页 |
| **角色** | 前端 |

### T-2.6 我的播客页

| 字段 | 内容 |
|------|------|
| **任务名称** | 我的播客页 |
| **目标** | 实现播客列表管理页 UI |
| **输入** | PRD §6.3 我的播客页：列表字段 + 筛选 |
| **输出** | `/podcasts` 路由列表页组件 |
| **依赖项** | T-1.4, T-1.8 |
| **完成标准** | ① 列表字段：标题/类型/状态/时长/字数/消耗积分/创建时间/操作 ② 筛选标签：全部/草稿/已完成 ③ 操作按钮：播放/继续编辑/下载/删除 ④ 空状态提示 ⑤ 分页或无限滚动 ⑥ 删除确认弹窗 |
| **模块** | 列表页 |
| **角色** | 前端 |

### T-2.7 登录注册页

| 字段 | 内容 |
|------|------|
| **任务名称** | 登录注册页 |
| **目标** | 实现登录和注册页面 UI |
| **输入** | PRD §10.3 认证接口：register（email/phone + password）、login（返回 JWT） |
| **输出** | `/login` 和 `/register` 路由组件 |
| **依赖项** | T-1.4, T-1.8 |
| **完成标准** | ① 登录页：邮箱/手机号 + 密码 + 登录按钮 + "注册账号"链接 ② 注册页：邮箱/手机号 + 密码 + 确认密码 + 注册按钮 + "已有账号"链接 ③ 表单验证（邮箱格式/密码长度/必填项） ④ 错误提示展示 ⑤ 响应式适配 |
| **模块** | 登录注册 |
| **角色** | 前端 |

### T-2.8 管理后台骨架页

| 字段 | 内容 |
|------|------|
| **任务名称** | 管理后台骨架页 |
| **目标** | 实现管理后台布局和占位页面 |
| **输入** | PRD §12 管理后台：9 个管理模块 |
| **输出** | `/admin` 路由后台骨架 |
| **依赖项** | T-1.4, T-1.8 |
| **完成标准** | ① 侧边栏导航含 9 个模块入口 ② 内容区占位 ③ 权限检查：非 admin 角色重定向到首页 ④ 顶部含管理员信息 + 退出按钮 |
| **模块** | 管理后台 |
| **角色** | 前端 |

---

## Phase 3：交互逻辑

> 目标：完成核心交互功能，使用 mock 数据

### T-3.1 登录注册交互（JWT 管理）

| 字段 | 内容 |
|------|------|
| **任务名称** | 登录注册交互（JWT 管理） |
| **目标** | 实现完整的登录注册流程和 Token 管理 |
| **输入** | PRD §10.3 认证接口 |
| **输出** | 前端 Auth 上下文 + 后端认证接口 |
| **依赖项** | T-2.7, T-4.5 |
| **完成标准** | ① 注册成功自动登录跳转 ② 登录成功跳转工作台 ③ JWT 存储在 localStorage + httpOnly cookie ④ Token 过期自动刷新 ⑤ 登出清除 Token ⑥ 未登录访问受保护页面重定向到登录页 ⑦ 后端 `POST /api/auth/register`、`POST /api/auth/login`、`POST /api/auth/refresh` 接口实现 |
| **模块** | 登录注册 |
| **角色** | 前端 + 后端 |

### T-3.2 创建播客交互（表单验证与预览）

| 字段 | 内容 |
|------|------|
| **任务名称** | 创建播客交互（表单验证与预览） |
| **目标** | 实现创建播客页的表单联动和预估积分计算 |
| **输入** | PRD §6.1 创建播客页 + §9.3 积分预估 |
| **输出** | 创建页完整交互逻辑 |
| **依赖项** | T-2.2 |
| **完成标准** | ① 单人/双人/长文解读模式切换联动 ② 双人模式显示角色 A/B 配置 ③ 预估积分实时计算（字数 × 1 积分/字 + 脚本生成 20 积分） ④ 余额不足时生成按钮置灰 + 提示 ⑤ 文本长度超限提示 ⑥ 音色试听功能 |
| **模块** | 详情页 |
| **角色** | 前端 |

### T-3.3 文稿编辑器交互（片段编辑 + 快捷工具栏）

| 字段 | 内容 |
|------|------|
| **任务名称** | 文稿编辑器交互（片段编辑 + 快捷工具栏） |
| **目标** | 实现编辑器核心交互：文本编辑 + 停顿/多音字/语气词标记插入 |
| **输入** | PRD §7.5 文本标记语言 + prototype/synthesis.html 工具栏 |
| **输出** | 编辑器完整交互逻辑 |
| **依赖项** | T-2.3 |
| **完成标准** | ① textarea 编辑后自动标记该段为 `draft` ② 快捷工具栏插入标记：短停顿 `<#0.5#>`、中停顿 `<#1.0#>`、长停顿 `<#2.0#>`、多音字 `【多音字】`、语气词 `(laughs)` ③ 多音字标记弹出选择器 ④ 字符计数实时更新 ⑤ 预计消耗积分实时更新 ⑥ 余额不足时合成按钮置灰 |
| **模块** | 详情页 |
| **角色** | 前端 |

### T-3.4 AI 优化片段交互

| 字段 | 内容 |
|------|------|
| **任务名称** | AI 优化片段交互 |
| **目标** | 实现一键 AI 优化片段功能 |
| **输入** | PRD §5.1 一键重新生成片段：🪄 AI优化按钮 + 浮层 + 快捷指令芯片 |
| **输出** | AI 优化浮层组件 + 交互逻辑 |
| **依赖项** | T-3.3 |
| **完成标准** | ① 点击"🪄 AI优化"弹出浮层 ② 浮层含快捷指令芯片："多举案例"/"缩短一点"/"更口语化"/"增加数据" ③ 含自定义指令输入框 ④ 提交后 LLM 生成新文本自动填入 textarea ⑤ 状态标记为 `draft` ⑥ 可取消/关闭浮层 |
| **模块** | 详情页 |
| **角色** | 前端 + 后端 |

### T-3.5 片段重合成与状态管理

| 字段 | 内容 |
|------|------|
| **任务名称** | 片段重合成与状态管理 |
| **目标** | 实现片段重合成的完整交互流程 |
| **输入** | PRD §7.2 文稿编辑与片段重合成 + §11.2 片段状态 |
| **输出** | 重合成交互逻辑 + 状态轮询 |
| **依赖项** | T-3.3, T-4.6 |
| **完成标准** | ① 修改文本后显示"重合成"按钮 ② 点击触发 `POST /api/podcast-segments/:id/resynthesize` ③ 状态变化：draft → queued → synthesizing → completed ④ 合成中显示 loading 动画 ⑤ 失败时显示"🔄 重试"按钮 ⑥ 重试最多 3 次，指数退避 ⑦ 合成完成自动更新音频播放器 |
| **模块** | 详情页 |
| **角色** | 前端 |

### T-3.6 删除片段与重新拼接

| 字段 | 内容 |
|------|------|
| **任务名称** | 删除片段与重新拼接 |
| **目标** | 实现删除片段 + 自动重新拼接 |
| **输入** | PRD §7.2 编辑动作：删除片段 → 删除音频后重新拼接（50ms crossfade） |
| **输出** | 删除交互 + 拼接触发逻辑 |
| **依赖项** | T-3.3, T-4.6 |
| **完成标准** | ① hover 显示"🗑️ 删除"按钮 ② 点击弹出确认对话框 ③ 确认后调用 `DELETE /api/podcast-segments/:id` ④ 自动触发 `POST /api/podcasts/:id/rebuild-audio` ⑤ 拼接期间显示进度提示 ⑥ 拼接完成后更新播放器 |
| **模块** | 详情页 |
| **角色** | 前端 + 后端 |

### T-3.7 音色切换与批量重合成

| 字段 | 内容 |
|------|------|
| **任务名称** | 音色切换与批量重合成 |
| **目标** | 实现角色音色替换后批量重合成 |
| **输入** | PRD §7.2 编辑动作：替换角色音色 → 该角色全部片段批量重合成 |
| **输出** | 音色切换交互 + 批量重合成逻辑 |
| **依赖项** | T-2.4, T-4.6 |
| **完成标准** | ① 右侧面板切换角色音色 ② 切换后弹出确认："更换音色后，该角色全部片段将重新合成，是否继续？" ③ 确认后调用 `POST /api/podcast-roles/:id/change-voice` ④ 该角色全部 segment 标记为 `draft → queued → synthesizing` ⑤ 逐段更新状态和进度 |
| **模块** | 详情页 |
| **角色** | 前端 + 后端 |

### T-3.8 音频播放器交互

| 字段 | 内容 |
|------|------|
| **任务名称** | 音频播放器交互 |
| **目标** | 实现底部音频播放器的核心交互 |
| **输入** | PRD §6.2 音频播放器：播放/暂停/进度条/下载/分享 |
| **输出** | 底部播放器组件 + 交互逻辑 |
| **依赖项** | T-2.3, T-4.7 |
| **完成标准** | ① HTML5 audio 播放/暂停 ② 播放当前片段 / 播放全部 切换 ③ 播放时自动滚动到当前片段并高亮 ④ 下载完整 MP3 按钮 ⑤ 分享按钮（V1.0 生成分享链接） ⑥ 导出前置条件检查（所有片段 completed 才可下载） |
| **模块** | 详情页 |
| **角色** | 前端 |

### T-3.9 我的播客列表交互

| 字段 | 内容 |
|------|------|
| **任务名称** | 我的播客列表交互 |
| **目标** | 实现播客列表的完整交互 |
| **输入** | PRD §6.3 我的播客页 |
| **输出** | 列表页完整交互逻辑 |
| **依赖项** | T-2.6, T-4.8 |
| **完成标准** | ① 筛选标签切换（全部/草稿/已完成） ② 点击播放按钮内嵌 audio 播放 ③ 点击"继续编辑"跳转编辑器 ④ 点击"下载"触发 MP3 下载 ⑤ 删除确认弹窗 + 调用 `DELETE /api/podcasts/:id` ⑥ 列表自动刷新 |
| **模块** | 列表页 |
| **角色** | 前端 |

### T-3.10 管理后台核心交互

| 字段 | 内容 |
|------|------|
| **任务名称** | 管理后台核心交互 |
| **目标** | 实现管理后台 9 个核心模块的交互 |
| **输入** | PRD §12 管理后台：用户列表/积分调整/积分流水/播客列表/任务列表/错误日志/音色配置/Provider配置/套餐配置 |
| **输出** | 管理后台各模块交互逻辑 |
| **依赖项** | T-2.8, T-4.9 |
| **完成标准** | ① 用户列表：查看/搜索/禁用/积分调整 ② 积分流水：按用户查看 + 筛选类型 ③ 播客列表：查看/删除 ④ 任务列表：查看状态/手动重试 ⑤ 音色配置：增删改音色 ⑥ Provider配置：切换 Primary/Fallback ⑦ 套餐配置：增删改套餐 |
| **模块** | 管理后台 |
| **角色** | 前端 + 后端 |

---

## Phase 4：数据存储与 API

> 目标：完成所有后端 API 实现，前后端联调

### T-4.1 认证 API 实现

| 字段 | 内容 |
|------|------|
| **任务名称** | 认证 API 实现 |
| **目标** | 实现完整的认证接口 |
| **输入** | PRD §10.3：register, login, logout, refresh |
| **输出** | 4 个认证接口 |
| **依赖项** | T-1.2, T-1.3 |
| **完成标准** | ① `POST /api/auth/register`：email/phone + password → 创建 user + credit_account（赠送 500 积分） ② `POST /api/auth/login`：返回 JWT access_token + refresh_token ③ `POST /api/auth/logout`：清除 token ④ `POST /api/auth/refresh`：刷新 access_token ⑤ 密码 bcrypt 加密 ⑥ 输入验证（邮箱格式/密码长度 ≥ 8） |
| **模块** | API 接口 |
| **角色** | 后端 |

### T-4.2 播客项目 CRUD API

| 字段 | 内容 |
|------|------|
| **任务名称** | 播客项目 CRUD API |
| **目标** | 实现播客项目的创建/读取/更新/删除 |
| **输入** | PRD §10.3：POST/GET/PATCH/DELETE /api/podcasts |
| **输出** | 4 个播客项目接口 |
| **依赖项** | T-1.2, T-4.1 |
| **完成标准** | ① `POST /api/podcasts`：创建播客项目（含 mode/style/target_duration） ② `GET /api/podcasts/:id`：返回项目详情 + 角色 + 片段 + 状态 ③ `PATCH /api/podcasts/:id`：更新标题/状态 ④ `DELETE /api/podcasts/:id`：软删除（status → deleted） ⑤ 权限检查：只能操作自己的项目 |
| **模块** | API 接口 |
| **角色** | 后端 |

### T-4.3 大纲生成 API

| 字段 | 内容 |
|------|------|
| **任务名称** | 大纲生成 API |
| **目标** | 实现 AI 生成大纲接口 |
| **输入** | PRD §7.1 AI 脚本生成 + §11.1 状态机（draft → outlining） |
| **输出** | 大纲生成接口 + LLM 调用逻辑 |
| **依赖项** | T-4.2 |
| **完成标准** | ① `POST /api/podcasts/:id/generate-outline`：触发大纲生成 ② 项目状态变更为 `outlining` ③ 调用 LLM 生成大纲（含播客标题/开场方式/3-5 核心段落/结尾总结） ④ 大纲生成完成自动进入 `scripting` 状态 ⑤ 失败回退至 `draft` 状态 ⑥ 大纲 MVP 只读不可编辑 |
| **模块** | API 接口 |
| **角色** | 后端 |

### T-4.4 脚本生成 API（含片段切分）

| 字段 | 内容 |
|------|------|
| **任务名称** | 脚本生成 API（含片段切分） |
| **目标** | 实现 AI 生成脚本并自动切分为片段 |
| **输入** | PRD §7.1 脚本字段 + §7.2 片段切分规则 + §9.3 脚本生成扣费 20 积分 |
| **输出** | 脚本生成接口 + 片段自动切分逻辑 |
| **依赖项** | T-4.3 |
| **完成标准** | ① `POST /api/podcasts/:id/generate-script`：触发脚本生成 ② 扣费 20 积分（固定） ③ LLM 生成脚本后按对话轮次/自然段自动切分为 segments ④ 每个 segment 绑定 role_id + sort_order + text + emotion ⑤ 单片段 ≤ 500 字，超长自动拆分 ⑥ 生成 `source_text_hash` ⑦ 项目状态变更为 `ready_to_synthesize` ⑧ 失败回退至 `draft` |
| **模块** | API 接口 |
| **角色** | 后端 |

### T-4.5 片段编辑与重合成 API

| 字段 | 内容 |
|------|------|
| **任务名称** | 片段编辑与重合成 API |
| **目标** | 实现片段的编辑、重合成、删除接口 |
| **输入** | PRD §7.2 编辑动作 + §10.3 片段编辑接口 |
| **输出** | 片段编辑/重合成/删除/重新拼接接口 |
| **依赖项** | T-4.4, T-1.5, T-1.6 |
| **完成标准** | ① `PATCH /api/podcast-segments/:id`：更新文本，标记 draft ② `POST /api/podcast-segments/:id/resynthesize`：触发单片段重合成，按字数扣费 ③ `POST /api/podcast-segments/:id/regenerate`：LLM 重新生成 + 合成 ④ `DELETE /api/podcast-segments/:id`：删除片段 + 触发重新拼接 ⑤ `POST /api/podcasts/:id/rebuild-audio`：重新拼接全部片段 ⑥ 合成前冻结积分，成功扣费，失败退回 |
| **模块** | API 接口 |
| **角色** | 后端 |

### T-4.6 合成任务 API（异步状态追踪）

| 字段 | 内容 |
|------|------|
| **任务名称** | 合成任务 API（异步状态追踪） |
| **目标** | 实现完整的异步合成任务流程 |
| **输入** | PRD §7.3 音频合成流程 + §11.1/§11.2 状态机 |
| **输出** | 合成任务接口 + Celery 任务实现 |
| **依赖项** | T-1.5, T-1.6, T-1.7, T-4.5 |
| **完成标准** | ① `POST /api/synthesis-tasks`：创建合成任务（冻结积分） ② `GET /api/synthesis-tasks/:id`：查询任务状态和进度 ③ `POST /api/synthesis-tasks/:id/retry`：重试失败任务 ④ Celery 任务：逐片段调用 TTS → 保存音频 → 拼接 → 上传 → 结算积分 ⑤ 片段级状态追踪（draft → queued → synthesizing → completed/failed） ⑥ Provider 降级策略（MiniMax → Edge-TTS） |
| **模块** | API 接口 |
| **角色** | 后端 |

### T-4.7 音色与试听 API

| 字段 | 内容 |
|------|------|
| **任务名称** | 音色与试听 API |
| **目标** | 实现音色列表和试听接口 |
| **输入** | PRD §6.4 声音工坊 + §10.3 音色接口 |
| **输出** | 音色列表 + 试听接口 |
| **依赖项** | T-1.2, T-1.5 |
| **完成标准** | ① `GET /api/voices`：返回预设音色列表（5 个） ② `POST /api/voices/preview`：试听接口（voice_id + text → 返回音频 URL） ③ `GET /api/voices/:id/emotions`：返回音色支持的情感风格 ④ 试听不扣费 ⑤ 角色批量换音色 `POST /api/podcast-roles/:id/change-voice` |
| **模块** | API 接口 |
| **角色** | 后端 |

### T-4.8 我的播客列表 API

| 字段 | 内容 |
|------|------|
| **任务名称** | 我的播客列表 API |
| **目标** | 实现播客列表查询接口 |
| **输入** | PRD §6.3 列表字段 + 筛选 |
| **输出** | 播客列表接口 |
| **依赖项** | T-4.2 |
| **完成标准** | ① `GET /api/podcasts`：返回当前用户的播客列表 ② 支持筛选：status（全部/草稿/已完成） ③ 支持分页 ④ 返回字段：标题/类型/状态/时长/字数/消耗积分/创建时间 ⑤ 按最近编辑时间排序 |
| **模块** | API 接口 |
| **角色** | 后端 |

### T-4.9 管理后台 API

| 字段 | 内容 |
|------|------|
| **任务名称** | 管理后台 API |
| **目标** | 实现管理后台所需全部接口 |
| **输入** | PRD §12 管理后台 9 个模块 |
| **输出** | 管理后台全部接口 |
| **依赖项** | T-4.1, T-4.2, T-4.6 |
| **完成标准** | ① 用户列表：`GET /api/admin/users`（搜索/分页） ② 积分调整：`POST /api/admin/credits/adjust` ③ 积分流水：`GET /api/admin/credits/ledger` ④ 播客列表：`GET /api/admin/podcasts` ⑤ 任务列表：`GET /api/admin/synthesis-tasks` ⑥ 错误日志：`GET /api/admin/error-logs` ⑦ 音色配置：CRUD /api/admin/voices ⑧ Provider配置：`GET/PATCH /api/admin/providers` ⑨ 套餐配置：CRUD /api/admin/plans |
| **模块** | API 接口 |
| **角色** | 后端 |

### T-4.10 数据埋点 API 与事件记录

| 字段 | 内容 |
|------|------|
| **任务名称** | 数据埋点 API 与事件记录 |
| **目标** | 实现基础埋点事件记录 |
| **输入** | PRD §12 数据埋点需求：11 个埋点事件 |
| **输出** | 埋点事件记录接口 + 事件表 |
| **依赖项** | T-1.2 |
| **完成标准** | ① `POST /api/events`：记录埋点事件 ② 新增 `analytics_events` 表（id/user_id/event/properties/created_at） ③ 支持 PRD 定义的 11 个事件 ④ 异步写入不阻塞主流程 ⑤ 管理后台可查看事件统计 |
| **模块** | API 接口 |
| **角色** | 后端 |

---

## Phase 5：用户系统与计费

> 目标：实现完整的积分计费闭环

### T-5.1 积分服务实现（冻结/扣费/退回）

| 字段 | 内容 |
|------|------|
| **任务名称** | 积分服务实现（冻结/扣费/退回） |
| **目标** | 实现积分生命周期管理 |
| **输入** | PRD §9.2 积分动作 + §8.2 credit_accounts 乐观锁 |
| **输出** | 积分服务类 + 接口 |
| **依赖项** | T-1.2, T-4.1 |
| **完成标准** | ① `freeze(user_id, amount, reference)`：冻结积分（乐观锁） ② `deduct(user_id, amount, reference)`：扣费（从冻结转扣费） ③ `refund(user_id, amount, reference)`：退回（从冻结退回） ④ `grant(user_id, amount, description)`：赠送（注册/每日登录） ⑤ 乐观锁更新：`UPDATE credit_accounts SET balance = balance - :amount, version = version + 1 WHERE id = :id AND version = :old_version` ⑥ 退回条件严格化：仅合成失败退回 ⑦ 异常行为检测：同一段 >3 次退回触发风控 |
| **模块** | API 接口 |
| **角色** | 后端 |

### T-5.2 积分余额与流水查询 API

| 字段 | 内容 |
|------|------|
| **任务名称** | 积分余额与流水查询 API |
| **目标** | 实现积分余额和流水查询 |
| **输入** | PRD §10.3 积分接口 |
| **输出** | 余额查询 + 流水查询接口 |
| **依赖项** | T-5.1 |
| **完成标准** | ① `GET /api/credits/balance`：返回 {balance, frozen, total_recharged, total_consumed} ② `GET /api/credits/ledger`：返回积分流水（分页 + 类型筛选） ③ 流水字段：type/amount/balance_after/reference_type/reference_id/description/created_at |
| **模块** | API 接口 |
| **角色** | 后端 |

### T-5.3 订单与套餐 API

| 字段 | 内容 |
|------|------|
| **任务名称** | 订单与套餐 API |
| **目标** | 实现订单创建和套餐配置 |
| **输入** | PRD §9.4 套餐包装 + §10.3 订单接口 |
| **输出** | 订单接口 + 套餐配置 |
| **依赖项** | T-5.1 |
| **完成标准** | ① `POST /api/orders`：创建订单 ② `GET /api/orders/:id`：订单详情 ③ `GET /api/orders`：订单列表 ④ 支付方式：card_key（卡密）MVP 优先 ⑤ 支付成功后积分入账 ⑥ 订单状态：pending → paid / failed |
| **模块** | API 接口 |
| **角色** | 后端 |

### T-5.4 前端积分展示与充值交互

| 字段 | 内容 |
|------|------|
| **任务名称** | 前端积分展示与充值交互 |
| **目标** | 实现前端积分展示和充值流程 |
| **输入** | PRD §9.4 套餐包装 + §3.2 预估消耗展示 |
| **输出** | TopBar 余额展示 + 充值页面 + 积分预估组件 |
| **依赖项** | T-5.2, T-5.3 |
| **完成标准** | ① TopBar 右侧显示余额（积分） ② 余额不足时高亮提示 ③ 充值页面展示 5 个套餐 ④ 卡密充值输入框 + 提交 ⑤ 积分预估组件：合成按钮旁实时显示"预计消耗: N 积分" ⑥ 余额 < 预估消耗时按钮置灰 |
| **模块** | 登录注册 / 详情页 |
| **角色** | 前端 |

### T-5.5 注册赠送积分 + 每日登录

| 字段 | 内容 |
|------|------|
| **任务名称** | 注册赠送积分 + 每日登录 |
| **目标** | 实现注册赠送 500 积分和每日登录 50 积分 |
| **输入** | PRD §9.3 积分获取表 |
| **输出** | 注册赠送逻辑 + 每日登录逻辑 |
| **依赖项** | T-5.1, T-4.1 |
| **完成标准** | ① 注册时自动调用 `grant(user_id, 500, "注册赠送")` ② 创建 credit_account 记录 ③ 每日登录：检查 `credit_transactions` 是否今日已有 `grant(type=daily_login)` ④ 每日登录 V0.3 评估是否上线（MVP 先实现逻辑，默认关闭） |
| **模块** | 登录注册 |
| **角色** | 后端 |

### T-5.6 音频资产生命周期清理

| 字段 | 内容 |
|------|------|
| **任务名称** | 音频资产生命周期清理 |
| **目标** | 实现过期音频资产自动清理 |
| **输入** | PRD §11.5 音频资产生命周期 |
| **输出** | 定时清理 Celery 任务 |
| **依赖项** | T-1.6, T-1.7 |
| **完成标准** | ① 片段音频（segment）：编辑新版本保留 7 天 ② 完整音频（full）：付费用户 90 天 / 免费用户 30 天 ③ 未完成音频：24 小时 ④ 定时任务每日凌晨执行 ⑤ 删除对象存储文件，数据库记录保留 ⑥ 日志记录清理数量 |
| **模块** | API 接口 |
| **角色** | 后端 + 运维 |

---

## Phase 6：集成联调

> 目标：前后端联调、端到端测试、部署上线

### T-6.1 核心闭环端到端联调

| 字段 | 内容 |
|------|------|
| **任务名称** | 核心闭环端到端联调 |
| **目标** | 跑通完整核心链路：注册 → 创建 → 生成 → 编辑 → 重合成 → 导出 |
| **输入** | 全部 Phase 1-5 产出 |
| **输出** | 可运行的端到端 Demo |
| **依赖项** | T-5.4, T-4.6, T-4.7 |
| **完成标准** | ① 新用户注册 → 获得 500 积分 ② 创建播客（单人/双人） → 生成大纲 → 生成脚本 ③ 编辑片段文本 → 触发重合成 ④ 使用快捷工具栏插入停顿/语气词 ⑤ 切换音色 → 批量重合成 ⑥ 删除片段 → 重新拼接 ⑦ 导出 MP3 下载 ⑧ 积分流水正确 |
| **模块** | 全局 |
| **角色** | 前端 + 后端 + 测试 |

### T-6.2 错误处理与边界场景覆盖

| 字段 | 内容 |
|------|------|
| **任务名称** | 错误处理与边界场景覆盖 |
| **目标** | 覆盖 PRD §11.3 全部失败场景 |
| **输入** | PRD §11.3 失败处理表 + §11.4 降级策略 |
| **输出** | 错误处理覆盖报告 |
| **依赖项** | T-6.1 |
| **完成标准** | ① 余额不足 → 禁止创建任务 ② 音色缺失 → 阻止合成 ③ TTS 超时 → 允许重试（指数退避） ④ 文本过长 → 引导拆句 ⑤ Provider 失败 → 降级至 Edge-TTS ⑥ 频繁重试 → 风控冻结 5 分钟 ⑦ 情感风格不支持 → 自动降级 ⑧ 文本标记解析失败 → 忽略标记 |
| **模块** | 全局 |
| **角色** | 后端 + 测试 |

### T-6.3 移动端适配与响应式优化

| 字段 | 内容 |
|------|------|
| **任务名称** | 移动端适配与响应式优化 |
| **目标** | 确保移动端流畅使用 |
| **输入** | PRD §15.4 移动端强制适配要求 |
| **输出** | 移动端适配完成 |
| **依赖项** | T-6.1 |
| **完成标准** | ① 编辑器页移动端单列布局 ② 触摸操作优化（滑动/长按） ③ 移动端音频播放（锁屏播放/后台播放） ④ 虚拟键盘适配 ⑤ 创建页表单移动端优化 ⑥ 列表页移动端优化 ⑦ 导航栏移动端汉堡菜单 |
| **模块** | 全局 |
| **角色** | 前端 |

### T-6.4 Docker 部署与生产环境配置

| 字段 | 内容 |
|------|------|
| **任务名称** | Docker 部署与生产环境配置 |
| **目标** | 完成生产环境部署配置 |
| **输入** | PRD §10.2 技术栈 + docker-compose.yml |
| **输出** | 生产级部署配置 |
| **依赖项** | T-6.2 |
| **完成标准** | ① Docker Compose 包含：前端/后端/PostgreSQL/Redis/MinIO/Celery worker ② Nginx 反向代理配置 ③ HTTPS 证书配置 ④ 环境变量管理（.env → .env.production） ⑤ 日志聚合配置 ⑥ 健康检查配置 ⑦ 数据库备份脚本 |
| **模块** | 全局 |
| **角色** | 运维 |

---

## 📊 任务依赖关系图

```
Phase 1：基础架构
T-1.1 ─→ T-1.2 ─→ T-1.5 ─→ T-4.5（片段重合成 API）
  │         │         │
  ├→ T-1.3  ├→ T-4.1  ├→ T-4.6（合成任务 API）
  ├→ T-1.4  ├→ T-4.2  └→ T-4.7（音色 API）
  ├→ T-1.6  ├→ T-4.3
  ├→ T-1.7  └→ T-4.4
  └→ T-1.8

Phase 2：静态页面（依赖 Phase 1）
T-1.4 + T-1.8 ─→ T-2.1 ~ T-2.8

Phase 3：交互逻辑（依赖 Phase 2 + Phase 4 部分 API）
T-2.x + T-4.x ─→ T-3.1 ~ T-3.10

Phase 4：数据存储与 API（依赖 Phase 1）
T-1.x + T-4.1 ─→ T-4.2 ─→ T-4.3 ─→ T-4.4 ─→ T-4.5 ─→ T-4.6

Phase 5：用户系统与计费（依赖 Phase 4）
T-4.1 ─→ T-5.1 ─→ T-5.2/T-5.3 ─→ T-5.4

Phase 6：集成联调（依赖全部）
T-all ─→ T-6.1 ─→ T-6.2 ─→ T-6.3/T-6.4
```

---

## 🏷️ 按模块索引

| 模块 | 任务编号 |
|------|---------|
| **登录注册** | T-2.7, T-3.1, T-5.5 |
| **首页** | T-1.4, T-1.8, T-2.1 |
| **列表页** | T-2.6, T-3.9, T-4.8 |
| **详情页（创建）** | T-2.2, T-3.2 |
| **详情页（编辑器）** | T-2.3, T-2.4, T-2.5, T-3.3, T-3.4, T-3.5, T-3.6, T-3.7, T-3.8 |
| **管理后台** | T-2.8, T-3.10, T-4.9 |
| **API 接口** | T-1.3, T-1.5, T-1.6, T-1.7, T-4.1~T-4.10 |
| **数据库设计** | T-1.2 |
| **计费系统** | T-5.1~T-5.6 |
| **部署上线** | T-6.3, T-6.4 |

---

## 🏷️ 按角色索引

### 产品
- 验收标准审核（每个任务的完成标准确认）
- 埋点事件定义审核（T-4.10）
- 核心闭环验收（T-6.1）

### 设计
- 设计系统审核（T-1.8）
- 页面 UI 审核验收（T-2.1~T-2.8）
- 移动端适配审核（T-6.3）

### 前端
- T-1.4, T-1.8, T-2.1~T-2.8, T-3.1~T-3.10, T-5.4, T-6.3

### 后端
- T-1.2, T-1.3, T-1.5~T-1.7, T-4.1~T-4.10, T-5.1~T-5.6, T-6.2

### 测试
- T-6.1（E2E 联调）
- T-6.2（错误场景覆盖）
- 各 Phase 产出的接口测试

### 运维
- T-6.4（Docker 部署）
- T-5.6（音频清理定时任务）
- 监控告警配置

---

## 📌 MVP 关键路径（Critical Path）

```
T-1.1 → T-1.2 → T-4.1 → T-4.2 → T-4.3 → T-4.4 → T-4.5 → T-4.6
                                                            ↓
T-1.1 → T-1.5 ─────────────────────────────────────────→ T-4.6
T-1.1 → T-1.6 ─────────────────────────────────────────→ T-4.6
T-1.1 → T-1.7 ─────────────────────────────────────────→ T-4.6
                                                            ↓
T-1.4 → T-2.3 → T-3.3 → T-3.5 ───────────────────────→ T-6.1
                                                            ↓
T-1.2 → T-5.1 → T-5.4 ───────────────────────────────→ T-6.1
```

**关键路径说明**：
1. **数据模型 → 认证 → 播客 CRUD → 大纲 → 脚本 → 片段 → 合成任务** 是最长的串行链路
2. **TTS 适配层 + 队列 + 存储** 可并行开发
3. **前端页面** 可与后端 API 并行开发（使用 mock 数据）
4. **积分系统** 需要在合成任务前完成

---

> 📌 本清单基于 PRD V3.0 生成，核心差异化功能（片段重合成/文本标记语言/情感风格/语言增强）均纳入 MVP。任务粒度控制在 1 个开发 session（约 2-4 小时）内完成。
